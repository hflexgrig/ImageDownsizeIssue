<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ImageDownsizeIssue.MainPage">
			 
    <ScrollView>
        <VerticalStackLayout>
            <Label>
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Original image: " />
                        <Span Text="{Binding OriginalSize}"
                              FontAttributes="Bold"
                              TextColor="Blue" />
                    </FormattedString>
                
                </Label.FormattedText>
            </Label>
            <Image Source="{Binding OriginalSource}" ></Image>
        
        
            <Label>
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Downsized image: " />
                        <Span Text="{Binding DownsizedImageSize}"
                              FontAttributes="Bold"
                              TextColor="Blue" />
                    </FormattedString>
                
                </Label.FormattedText>
            </Label>
            <Image Source="{Binding DownsizedSource}"></Image>
        
        </VerticalStackLayout>
    </ScrollView>
 
</ContentPage>
